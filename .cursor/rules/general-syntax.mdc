---
description: 练习项目通用语法与写法约定
alwaysApply: true
---

# 通用语法规则

本仓库用于个人练习。改代码时遵循下面约定，不要解释过程，只改该改的地方。

## 通用

- 用 4 空格缩进，不要用 Tab。
- 文件末尾保留一个空行。
- 不要把思考过程、计划或说明写进源文件。
- 用户用中文提问时，回复用中文；代码、路径、标签名保持原文。

## HTML

- 保留 `<!DOCTYPE html>`、`charset`、`viewport`。
- `lang` 与页面实际语言一致（中文用 `zh-CN`，英文用 `en`）。
- 浏览器标签用 `<title>`；页面可见标题用 `<h1>`，两者可以不同。
- 不要写空的无意义标签。需要内容时用语义标签（`h1`–`h3`、`p`、`ul`），不要用无意义的 `div` 堆叠。

```html
<!-- ❌ BAD -->
<title>Document</title>
<body><div>Hello World</div></body>

<!-- ✅ GOOD -->
<title>练习页</title>
<body>
    <h1>Hello World</h1>
</body>
```

## Markdown

- 一个文件一个一级标题 `#`。
- 代码块必须标明语言，例如 ` ```bash `、` ```html `。
- 列表、标题前后空一行。
